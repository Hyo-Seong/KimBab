<UserControl x:Class="KimBab.Controls.MenuSelectControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KimBab.Controls"
        d:DesignHeight="800" d:DesignWidth="1000"
        mc:Ignorable="d">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ListView Grid.Column="0" HorizontalAlignment="Left" Height="800" Width="500" VerticalAlignment="Top">
            <ListView.View>
                <GridView>
                    <GridViewColumn/>
                </GridView>
            </ListView.View>
        </ListView>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Button x:Name="AllBtn" Click="Menu_Click" FontSize="25" Content="전체" Width="90" Height="50" Margin="5,0,0,0"/>
                <Button x:Name="KimbabBtn" Click="Menu_Click" FontSize="25" Width="90" Content="김밥" Height="50" Margin="10,0,0,0"/>
                <Button x:Name="NoodleBtn" Click="Menu_Click" FontSize="25" Width="90" Content="면" Height="50" Margin="10,0,0,0"/>
                <Button x:Name="SiksaBtn" Click="Menu_Click" FontSize="25" Width="90" Content="식사" Height="50" Margin="10,0,0,0"/>
                <Button x:Name="BunsikBtn" Click="Menu_Click" FontSize="25" Width="90" Content="분식" Height="50" Margin="10,0,0,0"/>
                <Button x:Name="DongasBtn" Click="Menu_Click" FontSize="25" Width="90" Content="돈가스" Height="50" Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>


    </Grid>
</UserControl>
